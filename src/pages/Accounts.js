import React from "react";
import { Text } from "../components/base";

const Accounts = () => {
    return (
        <div className="flex items-center justify-center h-full">
            <Text as="h1" label="Account page" className="text-4xl font-extrabold" />
        </div>
    );
};

export default Accounts;
