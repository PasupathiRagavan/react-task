import React from "react";
import { Text } from "../components/base";

const Schedule = () => {
    return (
        <div className="flex items-center justify-center h-full">
            <Text as="h1" label="Schedule Page" className="text-4xl font-extrabold" />
        </div>
    );
};

export default Schedule;
